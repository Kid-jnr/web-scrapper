const cheerio = require('cheerio');
const fetch = require('node-fetch');

const url = "o2tvseries";
const options = {
    headers: {
      'User-Agent': 'request'
    },
    json:true
  };

class LoadMovies {
    constructor(){
        url
    }
}